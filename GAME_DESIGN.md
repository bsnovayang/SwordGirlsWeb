# Sword Girls 網頁復刻版 — 遊戲架構規劃書

> 目標：純 HTML + CSS + 原生 JS（無框架、無建置工具），雙擊 `index.html` 即可執行，
> 可直接放上 GitHub Pages。**無圖片、無音效，全部以文字／符號呈現。介面語系：繁體中文。**

---

## 0. 資料來源與考據摘要

原作《Sword Girls》（韓國 Zeonix 開發，2011 上線、2017 停止營運）為瀏覽器卡牌對戰遊戲。
以下規則整理自官方 Wiki 存檔（Game Rules / Character Cards / Dungeon / Deck Shop / 卡片頁面）。

**與一般 TCG 最大的不同：戰鬥是「同時下牌、自動結算」，玩家不控制攻擊目標。**
這點對實作非常有利 —— 不需要複雜的拖放指定目標，非常適合純文字版。

---

## 1. 核心規則（戰鬥系統）

### 1.1 卡片三種類型

| 類型 | 中文 | 數值欄位 | 說明 |
|---|---|---|---|
| Character Card | 角色卡 | 生命(Life，通常 30)、點數(Points) | 每副牌組**只能 1 張**，代表玩家本身，放在角色格。不能攻擊，但有能力。生命歸零即敗北 |
| Follower Card | 隨從卡 | 尺寸(SIZE)、攻擊(ATK)、防禦(DEF)、體力(STA)、點數(Points) | 上場作戰的單位，存活到 STA 歸零或被效果移除 |
| Spell Card | 法術卡 | 尺寸(SIZE)、點數(Points) | 發動後立即結算並進墓地 |

卡片其他屬性：**陣營(Faction)、稀有度(Rarity)、等級(Level)、章節(Episode/Season)、
牌組張數上限(Deck Limit，通常 3)、效果文字(Effect)、風味文字(Flavor Text)、合成配方(Recipe)**。

原版卡面數字位置（文字版可沿用同樣的排列邏輯）：
```
┌──────────────┐
│ 卡名      [4]│ ← 右上 = SIZE 尺寸
│              │
│ 效果文字...  │
│              │
│ 4    1    12 │ ← 左下 ATK / 中下 DEF / 右下 STA
└──────────────┘
```

### 1.2 四大陣營

四個可組牌的主陣營（台版譯名）：
`Vita 公立學校` / `Academy 私立學校` / `Crux 南十字` / `Darklore 暗黑族`
（背景：四方勢力共同對抗 Lazion 帝國）

另外還有兩類：
- **無所屬(Neutral)** — 任何牌組都能放的中立卡，繁中 wiki 有 Season 1／2 各一批
- **帝國(Empire)** — 韓服後期才加入的第五陣營，繁中 wiki 沒有收錄（台服應已先收攤），本專案不做

### 1.3 牌組規則

- 角色卡 **×1** ＋ 隨從/咒語卡 **共 30 張**
- 同名卡受該卡的 **Deck Limit** 限制（多數為 3 張，稀有卡 1～2 張）
- **陣營鎖定：牌組的陣營由角色卡決定，隨從與咒語只能放同陣營或「無所屬」的卡。**
  佐證：四副新手牌組都是純陣營；繁中 wiki FAQ 提到大廳棋子顏色
  「會依照使用中角色的所屬變化顏色」，且 DR 卡要「交換成自己勢力」才能用。
- 牌組有「牌組點數(Deck Points)」＝所有卡片 Points 加總，作為強度指標／限制

### 1.4 場地（★依原版截圖考據）

- 雙方各有 **5 個隨從/法術格(Slot I~V)** ＋ 1 個**角色卡格**
- 下牌時**必定填入編號最小的空格**
- 場上所有卡片 **SIZE 總和 ≤ 10**（被效果推超過 10 時不能再下牌，直到降回 10 以下）

**原版的實際排列**：畫面**左半＝我方、右半＝敵方**，各自的 5 格呈「C 字弧形」包住自己的角色卡，
弧形開口朝向畫面中央。編號自外上方順時針繞行：

```
        我  方                    敵  方
    ┌────┐┌────┐              ┌────┐┌────┐
    │ Ⅰ  ││ Ⅱ  │              │ Ⅱ  ││ Ⅰ  │
    └────┘└────┘              └────┘└────┘
┌────┐      ┌────┐        ┌────┐      ┌────┐
│角色││      │ Ⅲ  │        │ Ⅲ  │      │角色│
└────┘      └────┘        └────┘      └────┘
    ┌────┐┌────┐              ┌────┐┌────┐
    │ Ⅴ  ││ Ⅳ  │              │ Ⅳ  ││ Ⅴ  │
    └────┘└────┘              └────┘└────┘
              ↑ 畫面中央 ↑
```
- Ⅰ＝外上、Ⅱ＝內上、Ⅲ＝最內（最靠中央）、Ⅳ＝內下、Ⅴ＝外下
- 角色卡在弧形開口的外側正中

### 1.5 格位語意（實作務必嚴守）

多數卡片效果會指名格位，所以**格位編號是邏輯核心，不能只當成陣列順序**：

| 常見寫法 | 語意 |
|---|---|
| 「送到我方第一個空格」 | 由 Slot 1 往 5 掃描，填入第一個空格 |
| 「Slot 1、2、3 的敵方隨從」 | 絕對格位 |
| 「第一個我方隨從」 | 編號最小、且有卡的格位 |
| 「相鄰的隨從」 | 編號 ±1（弧形上視覺相鄰亦同） |
| 「最後一格 / Slot 5」 | 絕對格位 |

→ 實作上場地必須是**固定長度 5 的稀疏陣列**（`[card, null, card, null, null]`），
**絕對不可以用 push/splice 壓縮**，否則所有位置型效果都會壞掉。

### 1.6 回合流程（★依實機修正）

```
【抽牌】
  1. 雙方補牌至手牌 5 張
     ※ 若此時牌庫為 0 張 → 該玩家敗北
【下牌】(原版限時 30 秒 → 本版取消限時)
  2. 雙方同時、蓋著下牌（≤5 格、SIZE 總和 ≤10）
  3. 每場可使用 2 次「洗牌(Shuffle)」：手牌全數放回牌庫底，再抽同樣張數
【按下「確定」之後】
  4. 全場翻開
  5. 擲硬幣，決定這回合的先後
  6. 依序發動「回合開始時」能力：
       先手方：角色卡 → Ⅰ → Ⅱ → Ⅲ → Ⅳ → Ⅴ
       後手方：同上
【戰鬥】
  7. 雙方輪流「每次啟動 1 張牌」，直到場上所有牌都行動過
       - 啟動順序隨機，但【法術一定先於隨從】
       - 法術：結算效果 → 送入墓地
       - 隨從：攻擊「隨機一名」敵方隨從
【回合結束】→ 回到步驟 1
```

⚠️ **硬幣是按下「確定」之後才擲的，不是回合開頭。**
這代表「回合開始時」能力是在下牌之後才結算，
所以**本回合剛下場的牌也會吃到自己的「回合開始」能力** —— 例如
`料理研究社的見習生`「回合開始時，隨機一張『料理研究社』隨從攻/體+1」
在它上場的那一回合就會生效。若把硬幣與能力放在回合開頭，這類卡會整整慢一拍，是錯的。

（英文 wiki 的敘述含糊，看起來像回合開頭擲一次、下牌後再擲一次；
以實機畫面為準：TURN START 橫幅是在牌已下、計時剩 2 秒時才出現。）

### 1.7 傷害結算

```
傷害 = 攻擊方 ATK − 防禦方 DEF        （最低 0）
防禦方 STA −= 傷害
  ├ STA ≤ 0 → 該隨從被消滅，其「主人」的角色卡損失 Life = 該隨從的 SIZE
  └ STA > 0 → 防禦方【反擊】，用同一公式回敬攻擊方
※ **反擊 ＝ 額外的一次攻擊**，走跟主動攻擊完全一樣的流程：
　　· 被攻擊幾次就反擊幾次（只要沒死）
　　· 觸發反擊者的「攻擊前」能力
　　· 再觸發承受反擊那一方的「防禦前」能力
　　　（**「防禦前」不分主動攻擊或反擊，被打就觸發**）
　　· 反擊**不消耗**自己這回合的主動攻擊；沒被攻擊的卡一樣至少有一次主動攻擊

一次完整交戰的結算順序：
```
A 攻擊前 → B 防禦前 → A 打 B
   B 沒死 →  B 攻擊前 → A 防禦前 → B 反擊 A
```
※ 能力優先序（繁中 wiki FAQ）：發動時機相同時，效果文較前面的先處理；
　 且**「攻擊前」優先於「防禦前」**
※ 若敵方場上沒有任何隨從 → 直接攻擊敵方角色卡，傷害 = 攻擊方自己的 SIZE
```

### 1.8 勝負條件

1. 對手角色卡 Life 歸零
2. 對手在抽牌階段牌庫已空

---

## 2. 遊戲外圍系統

| 系統 | 內容 |
|---|---|
| **副本(Dungeon)** | 對 AI 戰。每個副本有數層樓，每層有指定敵人，最後一層為 Boss。**勝→上一層、敗→下一層、敗給 Boss→退回 1 樓**。每勝一場掉落合成素材。通關 10 次可取得 Boss 角色卡 |
| **副本難度分級** | Easy（新手/中級/高級迷宮、夢之島、竹林庭園）、Normal（繽紛世界、維塔公立學校、吸血鬼之地、莉妮亞宅邸、克魯司訓練場、魔女之塔、邊境遺跡）、Hard（暗影之地）、Extra（考場） |
| **合成(Craft)** | 每張卡有配方，用副本掉落素材合成。素材例：貓玩偶、綠礦石、白礦石、藍礦石、書、絲襪、鞋子、劍、眼鏡、聖獸之淚 |
| **商店(Shop)** | 卡包（依章節/陣營，30TK 一包）、新手牌組（各陣營 500TK）、卡背(5/50/100TK) |
| **變裝卡(Transformations)** | 同一角色的不同版本（洋裝希塔、劍道希塔、泳裝希塔…），視為不同角色卡 |
| **章節(Episode/Season)** | 卡池以章節劃分（Ep.0 ~ Ep.4、EX 等） |
| **PvP 對戰** | 原版有玩家對戰與排名。**本復刻版無伺服器 → 改為「與 AI 牌組對戰」的模擬天梯** |

---

## 3. 畫面架構（Screen Map）

單頁式應用（SPA）：所有畫面都是 `index.html` 內的 `<section>`，用 JS 切換 `display`。

```
[S1 標題畫面]
    └→ [S2 主選單]
            ├→ [S3 副本選擇] → [S4 副本樓層] → [S8 戰鬥] → [S9 結算]
            ├→ [S5 對戰(模擬天梯)]           → [S8 戰鬥] → [S9 結算]
            ├→ [S6 牌組編輯] ←→ [S7 卡片圖鑑]
            ├→ [S10 合成工房]
            ├→ [S11 商店]
            ├→ [S12 任務/成就]
            ├→ [S13 規則說明]
            └→ [S14 設定/存檔]
```

### S1 標題畫面
- **功能**：進入點、讀取存檔
- **UI**：遊戲標題 ASCII 字樣、`開始遊戲` / `讀取存檔` / `匯入存檔` / `遊戲說明` 按鈕、版本號

### S2 主選單（大廳）
- **功能**：導覽中樞、顯示玩家狀態
- **UI**：
  - 頂部狀態列：玩家名稱、等級、TK（金幣）、目前使用牌組名稱
  - 主選單按鈕列表（副本 / 對戰 / 牌組 / 圖鑑 / 合成 / 商店 / 任務 / 說明 / 設定）
  - 底部：跑馬燈式的提示文字或最近戰績

### S3 副本選擇
- **功能**：選擇要挑戰的副本
- **UI**：依難度分組的副本清單表格 —— 名稱｜難度｜總層數｜目前進度｜通關次數｜Boss 卡是否已取得

### S4 副本樓層
- **功能**：顯示副本內部進度、開始單場戰鬥
- **UI**：
  - 樓層直條圖（文字）：`▶ 3F  金獅諾爾德 (Boss)  HP30`
  - 目前所在層數高亮
  - 每層敵人名稱、生命、掉落素材預覽
  - `挑戰` / `離開副本` 按鈕

### S5 對戰（模擬天梯）
- **功能**：與預設 AI 牌組對戰，累積積分
- **UI**：對手清單（名稱｜陣營｜牌組點數｜勝率）、目前積分/段位、`挑戰` 按鈕

### S6 牌組編輯 ★重點畫面
- **功能**：組牌、切換使用中牌組
- **UI**：三欄式
  - 左欄：**牌組清單**（可新增/複製/刪除/改名/設為使用中）
  - 中欄：**卡片庫存**（含篩選器：陣營／類型／SIZE／稀有度／章節／關鍵字搜尋；排序）
  - 右欄：**目前牌組**
    - 角色卡格（1 張，點擊更換）
    - 卡片列表（卡名 ×張數，點擊減少）
    - 統計列：`30/30 張`、`牌組點數 xx`、`SIZE 分布長條圖`、`隨從/法術比例`
  - 合法性檢查提示（未滿 30 張、超過 Deck Limit、無角色卡）

### S7 卡片圖鑑
- **功能**：瀏覽全部卡片、查看詳細資料與配方
- **UI**：篩選列 ＋ 卡片表格 ＋ 右側詳情面板（全數值、效果、風味文字、合成配方、持有數／未持有以 `????` 顯示）

### S8 戰鬥畫面 ★★核心畫面（比照原版佈局）
- **功能**：執行 1.6 的完整回合流程
- **佈局原則**：**完全比照原版** —— 左半我方、右半敵方，各自 5 格弧形環繞角色卡，
  右側固定一塊「卡片詳情面板」，底部一列手牌 ＋ 控制鈕。

```
┌──────────────────────────────────────────────────────────┬──────────────────┐
│  我 方 場 地                    敵 方 場 地              │  ╔════════════╗  │
│  ┌───────┐┌───────┐        ┌───────┐┌───────┐            │  ║            ║  │
│  │軍事騎士││  Ⅱ   │        │  Ⅱ   ││ 妙族  ║            │  ║  卡 片     ║  │
│  │  ⟨4⟩  ││       │        │       ││觀測者 ║            │  ║  詳 情     ║  │
│  │ 6 1 2 ││       │        │       ││ 6 1 6 ║            │  ║            ║  │
│  └───────┘└───────┘        └───────┘└───────┘            │  ║  ♥ LIFE 9  ║  │
│┌───────┐      ┌───────┐  ┌───────┐      ┌───────┐        │  ╚════════════╝  │
│║露蒂卡 ║      │  Ⅲ   │  │  Ⅲ   │      ║妙族長 ║        │  露蒂卡‧普利班特  │
│║ ♥ 9   ║      │       │  │       │      ║ ♥ 5   ║        │  ────────────    │
│╚═══════╝      └───────┘  └───────┘      ╚═══════╝        │  角色卡          │
│  ┌───────┐┌───────┐        ┌───────┐┌───────┐            │  點數:10 上限:1  │
│  │  Ⅴ   ││  Ⅳ   │        │  Ⅳ   ││  Ⅴ   │            │  ────────────    │
│  └───────┘└───────┘        └───────┘└───────┘            │  【效果】        │
│                  ◆ 第 05 回合 ◆                          │  回合開始時，隨機 │
│                  《 回合開始 》                           │  一名克魯司隨從獲 │
├──────────────────────────────────────────────────────────┤  得 ATK+1/STA+1  │
│ sword0004  ▤15 ⚰10 ↻2      ▤17 ⚰8 ↻2  妙族觀測者        │  ────────────    │
├──────────────────────────────────────────────────────────┤  《風味文字》     │
│ 手牌 [卡1][卡2][卡3][卡4][  ]   ⏱—  SIZE 4/10  [確定][洗牌]│                │
└──────────────────────────────────────────────────────────┴──────────────────┘
```

**各區塊規格**

| 區塊 | 內容 |
|---|---|
| 場地格 (×10) | 空格顯示大寫羅馬數字 Ⅰ~Ⅴ 浮水印；有卡時顯示卡片 |
| 角色卡格 (×2) | 卡名 ＋ `♥ 目前生命`，陣營色邊框 |
| 中央區 | 回合數 `◆ 第 05 回合 ◆` ＋ 階段橫幅（回合開始／抽牌／下牌／戰鬥），階段切換時放大淡出 |
| 狀態列 | 左：我方 ID、牌庫數 ▤、墓地數 ⚰、剩餘洗牌 ↻；右：敵方同樣三項 ＋ 敵方角色名 |
| 手牌列 | 5 格；已下場的位置留空 |
| 控制列 | `SIZE n/10`、`確定(READY)`、`洗牌(SHUFFLE) ×剩餘`、`快轉`、`投降` |
| 右側面板 | 滑鼠移入任何卡片即顯示：卡名／類型／陣營／點數／牌組上限／效果／風味文字／（角色卡顯示 LIFE、隨從顯示 SIZE ATK DEF STA） |

**場上卡片的文字卡面**（比照原版數字位置）
```
┌─────────┐
│⚜   ⟨4⟩ │ ← 左上=陣營標記  右上=SIZE
│ 軍事騎士 │ ← 卡名
│ 席利特   │
│ 6  1  2 │ ← ATK / DEF / STA
└─────────┘
```

**狀態表現（無圖片，全靠文字/顏色）**
- **未翻開（本回合剛下的牌）**：整張卡灰階 ＋ 名稱顯示 `？？？`（敵方）
- **正在行動**：外框發光 ＋ 輕微放大
- **受傷**：STA 數字變紅並顯示 `12→9`
- **被消滅**：卡片淡出，中央訊息軸顯示 `✕ 軍事騎士席利特 被擊破（我方 −4 生命）`
- **數值被增益/減益**：該數字上色（增益綠、減益紅）並標 `+1` / `-1`

**互動**
- 下牌階段：點手牌 → 自動放入編號最小的空格；點我方場上卡 → 收回手牌
- SIZE 超過 10 的牌會變灰不可點，並提示 `SIZE 不足`
- 移入任何卡片 → 右側面板即時更新
- 戰鬥階段：事件逐一播放，速度可切 慢速／普通／快速／瞬間

**動畫**
| 時機 | 表現 |
|---|---|
| 擲硬幣 | 場地中央翻轉的金幣，停在「我」或「敵」那一面，下方標示先後 |
| 效果發動 | **該卡先亮一下（金色脈衝），才處理效果內容** |
| 數值增減 | 受影響的卡閃一下（增益綠、減益紫）＋ 跳出 `攻+2 體+2` |
| 攻擊 | 目標卡右上角起點，一道刀光往左下延伸 |
| 受傷 | `-3` 跳字往上飄後淡出（回血為綠色 `+n`） |
| 陣亡 | 卡片縮小傾斜、去彩度後淡出，**淡出播完才從畫面移除** |
| 懸停 | 跟隨游標的預覽小卡，顯示完整效果文字 |

**戰鬥紀錄**
中央區在戰鬥階段轉為捲動訊息軸，可用 `≡` 按鈕展開成完整 log 疊層。

### S9 戰鬥結算
- **UI**：勝／敗、回合數、造成傷害、掉落素材清單、獲得 TK、`再戰` / `返回`

### S10 合成工房
- **功能**：用素材合成卡片
- **UI**：左＝可合成卡片清單（依素材是否足夠標示 ✔/✘）；右＝配方明細（`貓玩偶 ×3 (持有 5)`）＋ 數量選擇 ＋ `合成` 按鈕；底部＝素材庫存一覽

### S11 商店
- **功能**：消費 TK
- **UI**：分頁 —— `卡包`（依章節/陣營，開包後逐張翻開的文字動畫）／`新手牌組`／`素材交換`

### S12 任務 / 成就
- **UI**：任務清單（名稱｜條件｜進度條｜獎勵｜領取按鈕），分「每日」與「成就」

### S13 規則說明
- **UI**：本文件第 1 章的圖解化說明，含分頁目錄

### S14 設定 / 存檔
- **UI**：文字速度、快轉預設值、`匯出存檔`（產生 JSON 字串供複製）、`匯入存檔`、`清除資料`

---

## 4. 技術架構建議

### 4.1 檔案結構
```
SwordGirlsWeb/
├── index.html          ← 唯一入口，包含所有 <section>
├── css/
│   └── style.css
├── js/
│   ├── data/
│   │   ├── cards.js        ← 卡片資料（const CARDS = [...]）
│   │   ├── dungeons.js     ← 副本、敵人牌組
│   │   ├── materials.js    ← 素材、配方
│   │   └── decks.js        ← 預設/新手牌組
│   ├── core/
│   │   ├── state.js        ← 全域狀態、存檔(localStorage)
│   │   ├── battle.js       ← 戰鬥引擎（純邏輯，產生事件序列）
│   │   ├── effects.js      ← 卡片效果註冊表
│   │   └── ai.js           ← AI 下牌決策
│   ├── ui/
│   │   ├── router.js       ← 畫面切換
│   │   └── screen_*.js     ← 各畫面渲染
│   └── main.js
└── GAME_DESIGN.md
```

> ⚠️ **重要**：因為要能用 `file://` 直接打開，**不可以用 `fetch()` 讀 JSON**（會被 CORS 擋）。
> 所有資料都要寫成 `.js` 檔用 `<script>` 載入，或直接內嵌。同理不能用 ES Module（`type="module"` 在 file:// 下也受限），建議用傳統 `<script>` ＋ 全域命名空間。

### 4.2 戰鬥引擎設計（關鍵）
`battle.js` 應為**純函式邏輯**，執行完一整個回合後回傳「事件陣列」，UI 再逐一播放：
```js
[
  {type:'phase',  name:'回合開始'},
  {type:'ability',source:'card_uid_3', text:'希塔·維洛沙：敵方 1~3 格 STA -1'},
  {type:'damage', target:'card_uid_9', amount:1, sta:[8,7]},
  {type:'attack', source:'card_uid_5', target:'card_uid_9', damage:3},
  {type:'destroy',target:'card_uid_9', lifeLoss:4},
  ...
]
```
好處：邏輯與演出解耦、好測試、快轉只是把延遲設為 0。

⚠️ **效果一律「先 say() 宣告，再做事」。** UI 在 `ability` 事件讓卡片亮起來，
代表「這張卡的效果要發動了」，之後才播送墓地／數值增減。順序寫反會先看到結果才看到亮光。
例如首席女僕的事件序列必須是 `ability → handGrave → ability → stat`，最後才輪到攻擊。

⚠️ **每個事件必須夾帶當下的「場面快照」**（`e.s`）。因為引擎是一次算完整回合才回傳事件，
UI 播放時若去讀即時狀態，會直接看到回合結束的畫面 —— 死掉的卡在播放前就消失了，
攻擊特效與陣亡動畫全部無效。`render(e.s)` 照快照畫才正確。

### 4.3 卡片效果系統
效果種類繁多，建議用「觸發時機 + 效果函式」註冊表：
```js
EFFECTS['100001'] = {                    // 希塔·維洛沙
  on: 'turnStart',
  run: (ctx) => { ctx.enemySlots(1,2,3).forEach(f => ctx.modify(f, {sta:-1})); }
};
```
第一版只實作少數關鍵字即可（STA/ATK/DEF 增減、消滅、直接傷害、回復、抽牌、位置移動）。

### 4.4 存檔
`localStorage` 儲存：玩家資料、卡片庫存、素材庫存、牌組、副本進度、任務進度。
另提供 JSON 匯出/匯入字串，方便換裝置或備份。

---

## 5. 開發階段建議

| 階段 | 內容 | 產出 |
|---|---|---|
| **P1** ✅ | 戰鬥引擎 ＋ 戰鬥畫面（四副新手牌組互打，無效果） | 已完成：300 場模擬 0 錯誤、jsdom 端到端通過 |
| **P2** ✅ | 卡片效果系統（Episode 0 有效果的 40 張全實作） | 已完成：105 個效果斷言通過 |
| **P3** ✅ | 大廳 ＋ 牌組編輯 ＋ 圖鑑 ＋ 存檔 | 已完成：46 個畫面斷言通過 |
| **P4** ✅ | 副本（Easy 三座）＋ 掉落 ＋ 合成 | 已完成：61 + 107 個斷言通過。商店依決策不做 |
| **P5** | 模擬天梯 ✅ · 任務與成就 ✅ · Episode 1 咒語 ✅ · 副本擴充到 5 座 ✅ · 手機版 ✅ · 平衡驗證 ✅ | 進行中 |

---

## 6. 已確認的決策

| # | 項目 | 決定 |
|---|---|---|
| 1 | 卡片規模 | 首版 **60~100 張**（四陣營各 15~25 張）＋ **8 張角色卡**，之後再擴充 |
| 2 | 命名 | **沿用原作台版譯名**（西塔・麥蘿莎、料理研究社的凱蒂、南十字騎士團的露西卡…），取自繁中 wiki |
| 3 | 下牌限時 | **取消**（原版 30 秒），改為玩家按「確定」才進戰鬥 |
| 4 | PvP | 改為 **AI 對手模擬天梯**（純前端無伺服器） |
| 5 | 視覺風格 | **現代簡潔卡片風**：淺色底、圓角、陣營色塊區分 |
| 6 | 戰鬥畫面 | **比照原版佈局**（左右分邊、弧形 5 格、右側詳情面板）—— 因為格位會影響效果，必須保留空間關係 |

### 陣營配色（風格 5 用）
| 陣營 | 主色 | 用途 |
|---|---|---|
| 公立學校 Vita | 暖橙 `#e8863c` | 卡片左側色條、邊框 |
| 私立學校 Academy | 藍紫 `#5b6ec9` | 同上 |
| 南十字 Crux | 天青 `#3fa9c9` | 同上 |
| 暗黑族 Darklore | 暗紫紅 `#8d4a7a` | 同上 |
| 中性 UI | 米白 `#faf7f0` / 文字 `#3a352e` | 底色 |

---

## 參考來源

- [Game Rules — Sword Girls Wiki](https://swordgirls.fandom.com/wiki/Game_Rules)
- [Character Cards — Sword Girls Wiki](https://swordgirls.fandom.com/wiki/Character_Cards)
- [Card Types — Sword Girls Wiki](https://swordgirls.fandom.com/wiki/Card_Types)
- [Dungeon — Sword Girls Wiki](https://swordgirls.fandom.com/wiki/Dungeon)
- [Deck Shop — Sword Girls Wiki](https://swordgirls.fandom.com/wiki/Deck_Shop)
- [Sword Girls — Wikipedia](https://en.wikipedia.org/wiki/Sword_Girls)
- [Sword Girls — NamuWiki](https://en.namu.wiki/w/%EC%86%8C%EB%93%9C%EA%B1%B8%EC%8A%A4)
- [Sword Girls — MMORPG.com](https://www.mmorpg.com/sword-girls)
